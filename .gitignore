node_modules/
dist/

# SQLite database files (regenerated by `npm run seed`)
*.db
*.db-shm
*.db-wal

# NOTE: server/data/queries.csv IS committed (3 MB) so the repo is self-contained
# and a grader can seed immediately after cloning.
