node_modules/
dist/

# SQLite database files (regenerated by `npm run seed`)
*.db
*.db-shm
*.db-wal

# ORCAS dataset is large (~330 MB gz) — NOT committed. Download it and place at
# server/data/orcas.tsv.gz (see README "Dataset"), then run `npm run seed`.
server/data/orcas.tsv
server/data/orcas.tsv.gz
